option = {
    title: {
        text: '//需要根据主题修改',
        left: 'center'
    },
    tooltip: {
        show: true
    },
    series: [{
        type: 'wordCloud',
        size: ['80%', '80%'],
        rotationRange: [-90, 90],
        rotationStep: 45,
        gridSize: 20,
        shape: 'circle',
        textStyle: {
            fontFamily: 'sans-serif',
            fontWeight: 'bold',
            color: function () {
                return 'rgb(' + [
                    Math.round(Math.random() * 160),
                    Math.round(Math.random() * 160),
                    Math.round(Math.random() * 160)
                ].join(',') + ')';
            }
        },
        emphasis: {
            textStyle: {
                shadowBlur: 10,
                shadowColor: '#333'
            }
        },
        data: [
            //需要动态填充数据
        ]
    }]
};